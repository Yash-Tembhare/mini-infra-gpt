cd ~/Documents/mini-infra-gpt

cat > requirements.txt << 'EOF'
# Core dependencies
requests==2.31.0
boto3==1.35.96
pyyaml==6.0.2
flask==3.1.0
pytest==8.3.4
python-dotenv==1.0.1
EOF

# Install dependencies
pip install -r requirements.txt